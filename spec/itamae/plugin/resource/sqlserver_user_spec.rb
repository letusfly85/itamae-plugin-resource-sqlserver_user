require 'spec_helper'

describe Itamae::Plugin::Resource::SqlserverUser do
  it 'has a version number' do
    expect(Itamae::Plugin::Resource::SqlserverUser::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
